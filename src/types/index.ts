export type TService = {
  path: string;
  title: string;
  description: string;
};
export type TSkills = {
  name: string;
  image: string;
};

export type Inputs = {
  name: string;
  email: string;
  subject?: string;
  description: string;
};
